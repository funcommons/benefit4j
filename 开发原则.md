# benefit4j 开发原则

> 效力范围:benefit4j 全部开发任务(backend / frontend / 数据库 / 测试 / documents)
> 性质:**强制约束**。与 `documents/` 各设计文档、ADR 冲突时先停下对齐,不得静默绕过。
> 日期:2026-08-25(派生自 TokenGo 开发原则 v2026-08-22,按 benefit4j 实际本地化)

---

## 原则一:后端一律使用 framework4j,禁止重复建设

### 1.1 能力映射(遇到下列需求,直接用 framework4j 对应模块)

> 本表仅列高频项;完整能力以 framework4j 仓 `docs/modules/` 为准,新模块发版后 1 周内补表。

| 需求场景 | framework4j 模块 | 禁止的自建行为 |
|---|---|---|
| 登录态 / JWT / Refresh 轮转 / 踢人 | `framework4j-accesstoken`(@RequiresToken、TokenPair) | 自写 JWT 签发校验、自管 refresh |
| 统一响应封装 / 错误码 | `framework4j-api` + `framework4j-web`(ApiResponse、GlobalExceptionHandler) | 自定义第二套信封/异常体系 |
| 接口签名防重放(开放/内部 API) | `framework4j-signature`(@RequiresSignature、HMAC-SHA256、nonce) | 自写签名拦截器 |
| 限流 | `framework4j-rate-limit`(@RateLimit、Lua 滑窗) | 自写限流器 |
| 缓存 | `framework4j-cache`(@CacheableGet/Put/Evict、Caffeine+Redis) | 自写二级缓存、自拼 Redis key |
| 幂等键 | `framework4j-idempotency`(Idempotency-Key、回放) | 自写防重复提交 |
| 审计日志 | `framework4j-audit`(@Auditable、Hash Chain) | 自写审计采集 |
| 字段脱敏 / 加密 | `framework4j-sensitive`(@Sensitive、AES-GCM TypeHandler) | 自写加密工具类 |
| 分布式 ID | `framework4j-id`(Snowflake、WorkerIdStrategy) | 自写 ID 生成器 |
| 多数据源 / 多 Redis | `framework4j-datasource` / `framework4j-redis` | 自写动态路由 |
| 时间处理 / SQL 追踪 | `framework4j-datetime` / `framework4j-sql-tracing` | 自写序列化器/追踪 Filter |
| 动态追踪日志(临时提权 DEBUG/TRACE) | `framework4j-tracelog`(v1.3.0+,已接入) | 自建日志采集/开关体系 |

**版本锁定**:以 `backend/pom.xml` 的 `<framework4j.version>` 为准(当前 v1.3.2,JitPack);业务 POM 禁写版本号。本文件不写死版本号——升级只改 parent。

### 1.2 framework4j 满足不了需求时——三条且仅三条出路

0. **优先回馈上游**:通用能力抽到 framework4j 发版实现(先例:通用能力回贡献 v1.2.9、tracelog v1.3.x),benefit4j 只引依赖。**自建件是最后手段,不是第一选择**;
1. **自己实现,但必须人为确认**:在任务/评审中写明「需求 → framework4j 现状差距 → 自建方案」,经确认后方可写代码;自建件统一放 `benefit4j-starter` 维护,禁止散落;
2. **提交 issue 要求 framework4j 升级**:登记差距与期望 API;framework4j 发版后**替换回框架实现,删除自建过渡件**。

无论走哪条路,都要在 `documents/adr/` 留一条决策记录(简版:差距/选择/回归计划;编号顺延,当前至 0009)。**禁止**:绕过确认直接自建、复制 framework4j 源码改巴改巴、同一能力两处各写一份。

---

## 原则二:优先复用本仓既有模式,不重复建设

- benefit4j 是**演进中的中台**,不是重写场:新域(如 assets)优先复用既有域验证过的模式,模式可搬、表结构按域独立;
- 判定阈值:单点改造成本 > 重写 50% 时才可选重写,且必须记 ADR;默认答案是复用改造。

### 2.1 复用源对照表(动手前先查)

| 源 | 角色 | 用法 |
|---|---|---|
| `documents/` 设计文档 + ADR | **行为先落文档**:产品说明书 / assets-design / dev-plan / ADR-0007~0009;修订必带版本号与修订记录 | 评审对照;文档↔代码 drift 视为未完成 |
| 既有权益域代码(`ubma_*`) | **模式复用真源**:CAS 乐观锁重试、TCC 预扣/结算/退款、`ReserveTimeoutScheduler` 过期回收、outbox、`@Auditable`/`@RequiresSignature` 用法 | 新域先对照,同模式直接搬,禁另起炉灶 |
| frontend `fc-web-sdk` + 既有 views | **前端复用**:FcTable / Pagination / FilterBar / 既有页面范式 | 新页面用 SDK 拼,禁重复造组件 |
| framework4j 仓 | 技术件真源(见原则一) | 缺口走 1.2 三条出路 |

### 2.2 复用判定流程(强制)

```text
接到需求
  → 查 documents/ 设计文档:已有定案?(含「明确不做」清单)
      ├─ 有 → 按定案实现;要做范围外事项先改文档再动手
      └─ 无 → 查既有域:有验证过的同型模式?
               ├─ 有 → 复用模式(表结构按域独立,禁止跨域共用表)
               └─ 无 → 走原则一 framework4j;仍无 → 1.2 三条出路
```

### 2.3 禁止行为(review 直接打回)

1. 既有域已有同型模式(CAS/TCC/outbox/审计)却从零重写,且无 ADR;
2. 命中设计文档「明确不做」清单的事项被变相实现(如 assets 域的批次三元组、周期发放);
3. 跨域共用表/共用唯一键硬拼语义(如拿权益桶当余额账本)。

### 2.4 豁免目录(视为第三方件等同物,零改动)

以下目录**不修改**:

- `frontend/src/views/dev` —— 开发者功能守护页(有可用性回归测试);
- `frontend/src/components/sdk` —— 通用 SDK 组件(跨项目复用件)。

需要变更时:在业务侧写适配层,或提 issue 走上游流程,**禁直接改这两个目录**。

---

## 原则三:四方对齐——对齐之前,任务不算结束

每个编码任务的 **Done 定义 = 文档、代码、测试、规范 四方对齐**:

| 方 | 对齐标准 | 检查方式 |
|---|---|---|
| 文档 | 行为先落 `documents/`(或修订现有章节),带版本号/修订记录 | 评审对照 |
| 代码 | 实现与文档一致;三模块职责不越界:`benefit4j-starter`(域逻辑)/ `benefit4j-app`(装配启动)/ `benefit4j-it`(集成测试) | Code review |
| 测试 | 单测 + 集成测试全绿(现有 60+ IT 无回归);新域并发/幂等/边界用例必配 | `mvn test`;JaCoCo(pom 已配) |
| 规范 | DB 相关过 `mc-database-spec` P0 五项(主键/禁 SELECT */追踪注释/JSONB GIN/禁外键);金额一律 NUMERIC,禁 double/float | P0 必查项 grep |

**任务关闭清单**(全部打勾才可关任务):

```text
□ 文档已更新(或确认无需更新)并追加修订记录
□ 代码与文档描述一致,模块职责未越界
□ 测试全绿(单测+集成,既有 IT 无回归)
□ 规范 P0 必查项通过
□ 涉及 framework4j 缺口的,已走 1.2 三条出路之一并留痕(ADR)
```

---

## 原则四:任务开始前,先扫 skills、尽量加载

任何任务动手前,先扫描 `.claude/skills/` 并按关键词加载相关技能,带着规范写,不要写完再补。**以当前仓实际清单为准,下表仅为典型映射;宁可多扫、不可漏载**:

| 任务类型 | 优先加载 |
|---|---|
| 任何后端编码 | 本仓现有:涉及流程编排 `mc-cli`;建表/SQL `mc-database-spec`(其余 mc-* 规范若安装则加载) |
| 建表 / 写 SQL | `mc-database-spec` |
| 测试编写 | 沿用 `benefit4j-it` 既有测试范式 |
| 前端页面 / 组件 | `fc-web-sdk` + 既有 views 范式(注意 §2.4 豁免目录) |
| 文档编写 | `documents/` 既有文档骨架(版本头/关联文档/评审要点) |

---

## 原则五:大任务分步交付,每步走"测试先行 + 评审循环"

### 5.1 分步规则

- 任务预估代码量**超过 1000 行**时,必须拆分,每步 ≤ 1000 行代码;
- 拆分按可独立对齐的最小闭环(一个聚合/一组接口/一个里程碑工作包),与 `documents/*-dev-plan.md` 的 WBS 对齐;
- **每步 = 一个 commit/PR**,禁跨步攒批;每步走完 5.2 且四方对齐(原则三)后才进入下一步。

### 5.2 每步固定流程(TDD + 评审循环)

```text
① 写测试代码(并发/幂等/边界用例先行)
② 写业务代码(实现到测试通过)
③ 跑测试(单测 + 集成,既有 IT 无回归)
④ 修复(直到全绿)
⑤ 评审循环:评审 → 出 P0~P2 级建议 → 执行 → 再评审
   退出条件:连续一轮无 P0~P2 建议,或已满 4 轮(仍遗留则记入任务清单并升级人为决策)
```

- P0~P2 分级:P0 正确性/资损/安全,P1 一期必做,P2 不阻塞但应记录;
- 每轮建议与执行结果留在任务记录中,**不得静默丢弃**;4 轮上限防停滞,不得无限循环。

---

## 原则六:资金/账本域强制项(assets 及后续资金类任务)

> 依据 [assets-design.md](./documents/assets-design.md) v0.4 定稿。凡是碰余额/分录/预扣的代码,以下为硬约束:

1. **append-only**:分录表(posting)应用层无 UPDATE/DELETE 入口;唯一合法变更 = 幂等撤销标记;
2. **DB CHECK 兜底**:余额非负/授信下限靠 schema 约束,**不靠应用自律**——任何「约定上不会为负」的口头保证无效;
3. **幂等闸独立表**:幂等唯一键不建在分区表上(分区表唯一约束必含分区键,会被稀释);独立小表 + 响应快照;
4. **锁序**:多腿事务按账户 id 升序 `FOR UPDATE`;边界户(发行/渠道/手续费)豁免锁与余额更新;
5. **对账必配**:资金操作 T+1 恒等式对账 + 差错池 + 告警,与功能同批交付,不允许「先上线后补对账」;
6. **合规 fail-fast**:FIAT 类资产未过合规评估,启动即拦(`assets.fiat-allowed=false`)。

---

## 项目专属约定

- `userid`(字符串,三方系统 ID)与 `user_id`(bigint 雪花 ID)**语义不同,不得合并或混用**;
- 汇报/说明用中文;代码、路径、命令、日志保留原文。

---

## 红线速查(review 直接打回)

1. 重复实现了 framework4j 已有能力(§1.1 表中"禁止的自建行为");
2. framework4j 缺口未经确认就自建(§1.2);可回馈上游的通用件堆进了业务仓;
3. 动手前未查 `documents/` + 既有域模式(§2.2);命中「明确不做」清单却变相实现(§2.3);
4. 修改了豁免目录 `frontend/src/views/dev` / `frontend/src/components/sdk`(§2.4);
5. 文档/代码/测试/规范任一方落后于其它三方就关闭任务(§3);
6. 未扫 skills 直接开写(§4);
7. 超 1000 行不分步、不按步 commit,或步骤不走"测试→实现→测试→修复→评审循环"(§5);
8. 资金类代码违反原则六任一条(无对账上线、分区表扛幂等键、应用层自律替代 CHECK)(§6)。
